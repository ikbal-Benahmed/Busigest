import { colors } from "./colors";
import { spacepx, space, lineHeights, lineHeightspx } from "./spacing";
import { sizespx, sizes } from "./sizes";
import { fontSizes, fontSizespx, fonts } from "./fonts";

export const theme = {
  fonts,
  colors,
  space,
  spacepx,
  lineHeights,
  lineHeightspx,
  sizes,
  sizespx,
  fontSizes,
  fontSizespx,
};
